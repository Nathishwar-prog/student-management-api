$kind: http-request
url: "{{baseUrl}}/api/enrollments/{{enrollmentId}}"
method: PUT
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "studentId": {{studentId}},
        "courseId": {{courseId}},
        "grade": "A+"
    }
order: 6000
